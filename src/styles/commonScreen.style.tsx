import {
    StyleSheet,
} from "react-native";

const commonScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignContent: 'center'
    },
    mainText: {
        fontWeight: '600',
        fontSize: 20,
        textAlign: 'center'
    }
});

export default commonScreenStyles;